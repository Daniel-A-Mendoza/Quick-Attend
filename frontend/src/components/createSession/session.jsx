import './session.css';
export const SessionQuestions = (flag) => {
    return (
        <div className={`content ${flag ? '.open' : ''}`}>
            
            <p>Create Session: </p>
        </div>
    );

};
